package co.bk.javaskills.patterns.factory.simplefactory;

/**
 * Factory implements this interface. 
 * 
 * Factories may implement abstract classes instead of interfaces. Abstract usage is an applicable approach 
 * when there is functional code that is shared between derived types.
 */
public interface Dog {

	public void talk();
}
